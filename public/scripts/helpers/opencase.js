


let repeat = false;
let rarityArray = [];
		let imageArray = [];
let randomAppend = () => {
	
	if (!repeat){
		
		for (i=0;i<randomArray.length;i++){
			let bet = randomArray[i] - 1;
			console.log(bet);
			//console.log(ItemsObject[0].items[10].rarity)
			rarityArray.push(ItemsObject[0].items[bet].rarity);
			imageArray.push(ItemsObject[0].items[bet].image);
			console.log(rarityArray);
			console.log(imageArray);
		}
		console.log(rarityArray);
		//let rarity = ItemsObject[0].items[randomArray].rarity;
		//let randomimage = ItemsObject[0].items[random].image;
		for (i=0;i<30;i++){
			var $newdiv = $('<div class="rarity-' + rarityArray[i] + ' card item"><img src="' + imageArray[i] + '"width="300px" height="300px"></div>');
			$('.owl-carousel').append($newdiv);
		}
	}
	else{
		
		var $newdiv = $('<div class="rarity-' + rarityArray[29] + ' card item"><img src="' + imageArray[29] + '"width="300px" height="300px"></div>');
		$('.owl-carousel').append($newdiv);
	}
	//let random = randomArray; //randomArray is generated by calculatewin.js and passed from app.js. Results in random weighted.
	

	

};

function sleep (time) {
	return new Promise((resolve) => setTimeout(resolve, time));
  }
randomAppend();


let winner = parseInt(winnerItem);
winner = winner - 1;
console.log("WINNER: " + winner);
let raritycss = 0;
let b_color = '';
switch (ItemsObject[0].items[winner].rarity){
	case 1: 
	raritycss = 1;
	b_color = 'gray';
	break;
	case 2: 
	b_color = 'green';
	raritycss = 2;
	break;
	case 3: 
	b_color = 'blue';
	raritycss = 3;
	break;
	case 4: 
	b_color = 'purple';
	raritycss = 4;
	break;
	case 5: 
	b_color = 'pink';
	raritycss = 5;
	break;
}
let winImage = ItemsObject[0].items[winner].image;
var $windiv = $('<div class="rarity-' + raritycss + ' card item"><img src="' + winImage + '"width="300px" height="300px"></div>');
$('.owl-carousel').append($windiv);
repeat=true;
randomAppend();
let winnerPrice = ItemsObject[0].items[winner].price;





$(document).ready(function () {
	
	var owl = $('.owl-carousel');
	owl.owlCarousel({
		loop: false,
		margin: 10,
		touchDrag: false,
		mouseDrag: false,

		responsive: {
			0: {
				items: 1
			},
			600: {
				items: 2
			},
			1000: {
				items: 3
			}
		}
	});


	$('.btnJump').click(function () {
		for (i = 0; i < 32; i++) {
			owl.trigger('next.owl.carousel', [6000]); //6 seconds
			console.log("Triggered" + i);
		}
		sleep(6000).then(() => { //6 seconds to match the roll
			$('.win-text').text(winningString + ' ' + "(" + winnerPrice + ")");
			swal({
				title: 'Sweet!',
				html: '<h2 style="color:black">' + winningString + ' ' + winnerPrice +  '</h2>',
				imageUrl: winImage,
				imageWidth: 450,
				imageHeight: 350,
				background: b_color,
				imageAlt: 'Custom image',
				animation: false
			  });
		});
		


	});

});